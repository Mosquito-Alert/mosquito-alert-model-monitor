# Example crontab entries for Mosquito Alert model monitoring

# Run species classification model daily at 8:30 AM
30 8 * * * /path/to/mosquito-alert-model-monitor/scripts/example_model_job.sh

# Run data preprocessing pipeline daily at 6:00 AM  
0 6 * * * /path/to/your/data_preprocessing_job.sh

# Run risk prediction model twice daily at 10:00 AM and 4:00 PM
0 10,16 * * * /path/to/your/risk_prediction_job.sh

# Update dashboard every 30 minutes (optional - if not using GitHub Actions)
# */30 * * * * cd /path/to/mosquito-alert-model-monitor && quarto render && git add docs/ && git commit -m "Update dashboard" && git push

# Clean up old log files weekly
0 2 * * 0 find /path/to/mosquito-alert-model-monitor/data/history -name "*.json" -mtime +30 -delete
